# four-pools review (round 1)

Rendered at 820 and 1100 px and inspected with zoom crops (`/tmp/diagram-png/c1`–`c6`). The provided `/tmp/render-diagram.py` produced a broken-image placeholder (Chromium blocks `file://` subresources from a `set_content` page; a `page.goto` variant works — harness fix, not counted here). All geometry claims verified against rect/path coordinates in `assets/diagrams/four-pools.svg`.

## P1

- [颜色搭配] 9 of 11 flow-label chips render as blank white pills — white-on-white: "Mints POL", "~2/7" (pol-to-polpool), "~3/7", "~2/7" (pol-to-ptpol), "uAsset ~2/3", "uAsset ~1/3", "PT ~1/3", "PT ~2/3", "YT". SVG CSS: `.c-mask { fill: #ffffff }` pill + `.t-muted { fill: #ffffff }` text; only the two `t-backend` (#34d399) emphasis chips (~70%, ~30%) survive. The diagram's entire ratio layer — the 2:3:2 and 1:2 splits it exists to explain — is invisible at both 820 and 1100 → give default edge labels dark text (or dark pill); export script theme (same `.c-mask`/`.t-muted` bug as fee-split), not src JSON.
- [颜色搭配] All node text is white on pastel fills: nodes are a white `c-mask` rect under a 40% tint, so effective fills are (183,165,213) database / (155,184,177) backend / (142,148,157) external, while `t-primary`/`t-muted`/`t-external` are all `#ffffff` — ~2:1 contrast. Worst: "~70% · main trading market", "~30%", "Claimable from Locked, permanently", "Preorder & interest excluded" → darken node text or drop the white underlay; export script theme.
- [字体] Node sublabels are SVG `font-size="7"` → 5.3 px effective at the 820 px docs width (flow chips 8 → 6.1 px, stage headers 9 → 6.8 px, node titles 10 → 7.6 px). The sublabels carrying "~70%", "YT pro rata; remainder to leverage" etc. are below legible threshold at page size — the recurrence of the user's original complaint → raise preset font scale or render the 1080 viewBox nearer 1:1; export script (src JSON has no font knob).
- [图形错误] Type sigil overlaps the first letter on "Primary Memecoin/uAsset", "Primary pool mints POL", and "Auxiliary uAsset" — sigil sits at node.x+6/y+6 (e.g. mainpool sigil x246–257, title spans x246–384 at baseline 263) and collides wherever the centered title is nearly box-wide → offset the title anchor or move the sigil out of the title band; export script (sigil placement).
- [图形错误] The "YT" chip (rect 831–865 × 377–393) covers the 824→872 arrow to "Genesis participants' YT" and merges with its white arrowhead into one white blob — the only YT-distribution arrow on the diagram is swallowed (zoom crop c2) → move the label off the segment (labelDy above the line) or shorten the pill; src JSON (`labelDy`) or renderer hit-test.
- [排列布局] Long empty bands, matching the user's screenshot: bottom-left quadrant (x≈24–830, y≈430–560) is completely empty except the y=560 rail; stage-01 lane has one 152-px node and ~280 px of dead column above it; node ink ≈ 11% of the 1080×640 canvas → rebalance rows (funds at row 1, pull row 3 up) and shrink `meta.viewBox` to ~1080×520; src JSON (`row`/`stage`/`viewBox`).
- [排列布局] "uAsset ~1/3" pill (467–533 × 76–92) floats detached above its own line (route at y=94) and directly under the "03 / POL mint & auxiliary funding" header — reads as a header badge, not a flow label (crop c6); the bottom "~2/7" pill (683–717 × 565–581) similarly straddles the dashed lane border below its y=560 line (crop c5) → set `labelAt` onto each line segment; src JSON.
- [产品语义] Card "How to read the ratios" asserts "the PT split's rounding difference goes to the PT/POL side" — four-pools.md says the opposite: "The small remainder of POL/PT that does not enter a pool is claimed pro rata after unlock" (to participants, not into a pool); no doc routes rounding to PT/POL → reword to the pro-rata-after-unlock rule; src JSON (card item).
- [产品语义] Flow labels "uAsset ~2/3" / "uAsset ~1/3" and the matching card item "(~2/3 into POL/uAsset, ~1/3 into PT/uAsset)" state an auxiliary-capital split that appears nowhere in four-pools.md or any memeverse/reference doc — the docs only say the ~30% "enters the POL/uAsset and PT/uAsset pools". Fabricated precision on the three most eye-catching labels → drop the ratio or ground it in a doc first; src JSON.

## P2

- [产品语义] ytdist sublabel "Claimable from Locked, permanently" — "From Locked" is grounded (lifecycle.md:15) but "permanently" appears in no doc; docs describe YT residual redemption at settlement instead → drop "permanently" or cite a source; src JSON.
- [产品语义] The 365-day lock — a defining fact of the four-pool structure (README.md:17, lifecycle.md:62) — appears nowhere on the diagram; natural home is the primary-pool sublabel ("~70% · locked 365 days") or the stage-03 rail; src JSON.
- [箭头形状] Two parallel vertical rails 10 v-px apart at x=1050 (pol-to-polpool) and x=1060 (aux-to-ptpool) run the full right edge — a ~7.6 px gap at 820 px with two same-styled lines; ownership is ambiguous until each arrowhead → separate the via x-coordinates (e.g. 1035/1070); src JSON (`via`).
- [箭头形状] pol-to-ptpol hugs the lane boundary: its y=560 horizontal rides just above the dashed stage-border and its x=530 drop runs ~260 v-px through empty lane space, so the route reads as a lane border rather than a flow until the up-arrow into PT/POL (crop c5) → route it through a clear lane (e.g. drop at x=560, horizontal at y=470); src JSON (`via`).
- [字体] "Primary pool mints POL" title is squeezed to font-size 9.3 (7.1 px at 820) vs 10 on all siblings — the smallest node title on the canvas, on one of the most important nodes → widen the node (width 132) or shorten the label; src JSON.
- [颜色搭配] Legend label "primary data" for the green emphasis arrows is preset jargon — the arrows carry the ~70%/~30% capital split, not data → override via `meta.legend.entries.emphasis.label` (e.g. "Capital split"); src JSON.
