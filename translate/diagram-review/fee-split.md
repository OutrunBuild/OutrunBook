# fee-split review (round 1)

Rendered at 820 and 1100 px (inline-SVG render; the provided `/tmp/render-diagram.py` produces a broken-image placeholder for every diagram because Chromium blocks `file://` subresources from a `set_content` page — fix the harness, not counted here). Geometry claims below verified against rect coordinates in `assets/diagrams/fee-split.svg`.

## P1

- [图形错误] All 7 default-variant flow labels render as solid white pills with invisible white-on-white text: "With referrer: default 10% of total", "Split by denomination", "Auxiliary-pool uAsset/PT fees (Locked phase)", "uAsset-denominated", "Memecoin-denominated", "POL-denominated", "Standard side: pro rata by Genesis share". SVG CSS: `.c-mask { fill: #ffffff }` (label pill) + `.t-muted { fill: #ffffff }` (label text); only the two emphasis labels survive because they use `.t-backend` (#34d399). The diagram's entire explanatory layer is blank white bars at both 820 and 1100 → give default edge labels a dark text color (or dark pill); fix in export script theme (`.c-mask`/`.t-muted` pairing in `render-dataflow.mjs` renderFlowLabel), not src JSON.
- [图形错误] Node sigil overlaps the title's first glyph on 7 of 10 nodes ("One trading fee", "By denomination", "Protocol share", "Aux-pool split", "Burned directly", "Genesis participants", "Staking YieldVault") — icon is placed at `node.x+6, node.y+6` inside the same band as the centered title, so any title long enough to reach x+14 collides → reserve horizontal space for the sigil (offset title anchor or place sigil above the title row); export script (`renderSemanticSigil` placement in `render-dataflow.mjs`).
- [字体] Every edge label and node sublabel is SVG `font-size="8"` on a 1080-wide viewBox → ~6.1 px effective at the 820 px docs width (node titles 10 → 7.6 px, stage headers 9 → 6.8 px); at real page size the 65%/35% facts and all sublabels are below legible threshold → raise preset font scale or render viewBox nearer 1:1 at 820; export script (renderer font-size constants; src JSON has no font knob).
- [产品语义] "Referral rebate" and "Aux-pool split" sit in the stage rail "03 / Dimension 2: by denomination", but per business-model.md the rebate is carved out of the protocol's 35% under Dimension 1 ("the referral rebate is carved out of the protocol's 35%"), and aux-pool sharing is a separate Locked-phase detail ("One more detail"), not denomination routing — the diagram's own card states the carve-out from the protocol share, contradicting the lane it is drawn in → move `referrer` (and `auxsplit`) to stage 1 or relabel the stages; src JSON (`stages` / node `stage` fields).

## P2

- [排列布局] "POL-denominated" label (rect 697.25,421–782.75,437) floats directly beneath the "Burned directly" node (bottom y=414) while its edge runs at y=340 and enters the node from the top — orphan label attached to nothing, reads as a caption of the node → move `labelAt` beside the y=340 segment (e.g. ~[640,340]); src JSON.
- [排列布局] "uAsset-denominated" pill (x 589.9–690.1) clips the Staking YieldVault node's left border (x=689) by 1.1 px — white corner sits on the violet stroke → nudge `labelDy`/anchor or shrink pill; src JSON (labelDy 51) or renderer hit-test.
- [产品语义] Aux flow label "Auxiliary-pool uAsset/PT fees (Locked phase)" omits the POL/uAsset pair; doc: "auxiliary pools (POL/uAsset, PT/uAsset, PT/POL)" → "Auxiliary-pool POL/uAsset/PT fees"; src JSON (flow label + matching card item).
- [产品语义] DAO treasury sublabel "0.25% carved out · default" is ambiguous — reads as "DAO receives 0.25%"; doc: 0.25% of primary-pool uAsset fees goes to the executor, carved from within the uAsset→DAO route → reword to "executor 0.25% carved · default" or similar; src JSON.
- [颜色搭配] Legend wording "primary data" for the green emphasis arrows is preset jargon meaningless for a money-flow diagram (the arrows carry the 65%/35% fee split, not "data") → override via `meta.legend.entries.emphasis.label` (e.g. "Main fee split"); src JSON (falls back to renderer i18n string).
- [颜色搭配] White title/sublabel text on the pale green backend fills (`t-primary`/`t-muted` #ffffff over `rgba(6,78,59,0.4)` atop the white `c-mask`) — sublabels like "uAsset/Memecoin/POL" and "Deflationary" are washed out and low contrast on all four green nodes → darken the effective node fill or switch node text to a dark tint; export script theme.
