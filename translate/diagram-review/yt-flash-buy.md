# yt-flash-buy review (round 1)

Evidence: current `assets/diagrams/yt-flash-buy.svg` (mtime 03:12), rendered at 820 and 1100 via `<img>` wrapper (`/tmp/diagram-png/yt-flash-buy.png`, `yt-flash-buy-1100.png`); SVG CSS parsed directly. Semantic truth: `memeverse/yt-flash-swap.md` BUY direction. Note: three of the P1s are export-script defects also present in `yt-flash-sell.svg` (checked).

## P1

- [图形错误 / 箭头形状] "All marker polygons (`polygon points='0 0, 10 3.5, 0 7' class='m-default/m-emphasis/...'`) have no fill anywhere in the SVG CSS — no `.m-*` rules exist, so they default to black — every arrowhead (4 messages + 3 legend samples) renders as a black triangle on the near-black background and is invisible; arrows read as naked lines" → emit explicit `fill` on the `.m-default/.m-emphasis/.m-dashed/.m-security` marker polygons (green for emphasis, light slate otherwise) + export script
- [箭头形状 / 排列布局] "The four per-edge CSS overrides are dropped: `data-composition-points="69,185;163,185"` was exploded into the invalid selector `[163,185=""]` (4 occurrences), which invalidates each whole rule — every message falls back to `path.a-default {stroke-width:.8px; dasharray 3,7}`, so 'Put up y POL to split' (a request, legend says solid 1.4px) renders as a thin dashed line identical to the lifelines, the two return arrows lose their 1.4px/3,5 return styling, and none of the legend's three samples matches how any message actually renders" → stop deriving selectors from `data-composition-points` (drop that attr from the selector or sanitize `;`), keep base `.a-emphasis/.a-default` rules self-sufficient + export script
- [图形错误 / 产品语义错误] "All three annotation notes are invisible in the static SVG: `text.t-dim[data-detail="fine"] { … opacity: 0 }` hides 'These y PT are "borrowed": you hold none yet' (y=203), 'Your own funds + R → y PT + y YT' (y=253) and 'You actually pay y − R POL' (y=348) — the y − R cost math, the diagram's punchline, is gone from the rendered artifact" → bake notes visible at normal opacity in the SVG export (reserve opacity toggling for the HTML viewer) + export script
- [排列布局] "viewBox is 820 wide but every element sits inside x≈19–368 (participant boxes x 19–321, arrows span x 62–278, legend ends ≈368): the right ~55% of the canvas is empty grid, the three lifelines are squeezed into the left third, and message spans are tiny relative to the canvas" → shrink viewBox to fit content (~400×500) or distribute participants across the width + src JSON (viewBox [820,500]) | export script
- [字体] "At the native 820px size the type is too small to read: message labels 9px, participant sublabels 6.5/6.8/7px ('1 POL = 1 PT + 1 YT' — the pricing rule — at 6.8px), legend items 10px, notes 7px (when visible); participant names are only 11px" → raise the SVG text scale (~12–13px labels, ~10px sublabels/notes), or shorten labels in src so they can be set larger at the same width + src JSON | export script
- [产品语义错误] "The chapter's headline fact — the whole swap is one atomic transaction — appears nowhere in the rendered SVG: `meta.title` ('Buy YT: borrow PT → split → repay PT (one atomic transaction)') is not drawn, and the three cards (maximum-pay cap semantics, one fee on the full PT-leg size, Locked-phase-only + smart-account precondition) exist only in the HTML page while the docs embed the bare SVG; step order borrow→split→repay→receive and the maximum-pay-cap sublabel are correct, but atomicity/caps/fees coverage is lost in the embed" → render the title inside the SVG and either inline condensed cards/footnotes into the SVG or accept the loss explicitly + export script | HTML

## P2

- [排列布局] "Lifelines (x=62/170/278, y 142→435) run down into the legend block: x=62 passes straight through the word 'Legend' (title baseline y=426, spans x≈40–84) and all three dangle between the title and the items row (y=446)" → end lifelines above y≈410 or give the legend a masked backdrop + export script
- [排列布局] "'Repay with the y PT from the split' chip (mask x 21.6–210.4) overhangs 40px left of the User lifeline (x=62) and 47px left of its own arrow start (x=69), sitting nearly flush with the canvas edge — the only label that hangs outside its arrow span" → center the chip over the actual arrow span (or wrap to two lines) + export script
- [颜色搭配] "'Put up y POL to split' gets a green label (`t-backend`, same green as the emphasized borrow) while its arrow is the default gray style — a color the legend only defines for the solid green 'request' sample, so the label promises an emphasis the line doesn't deliver" → key chip text color to the message `variant` (green only for `emphasis`) + export script
